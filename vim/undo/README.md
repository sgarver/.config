# PERSISTENT UNDO SETUP

Vim can store undo history on disk so it persists between sessions.

To enable this create the undo directory (run once on a new machine):

```sh
mkdir -p ~/.config/vim/undo
```
