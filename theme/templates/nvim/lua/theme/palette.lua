local M = {}

-- values injected by your theme generator
M.colors = {
  bg     = "${BG}",
  fg     = "${FG}",
  red    = "${RED}",
  yellow = "${YELLOW}",
  green  = "${GREEN}",
  blue   = "${BLUE}",
  purple = "${PURPLE}",
  gray   = "${GRAY}",
}

return M
